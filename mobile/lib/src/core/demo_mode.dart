const testerLoginEnabled = bool.fromEnvironment(
  'ENABLE_TESTER_LOGIN',
  defaultValue: false,
);

const testerAccessToken = 'tester-demo-access-token';
const testerRefreshToken = 'tester-demo-refresh-token';
